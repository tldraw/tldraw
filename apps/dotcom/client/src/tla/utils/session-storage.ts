export const SESSION_STORAGE_KEYS = {
	REDIRECT: 'redirect-to',
	WORKSPACE_INVITE_TOKEN: 'workspace-invite-token',
	SHARE_LINK_OPENED: 'share-link-opened',
	STALE_CHUNK_RELOAD: 'stale-chunk-reload',
} as const
