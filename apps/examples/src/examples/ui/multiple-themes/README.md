---
title: Multiple themes
component: ./MultipleThemesExample.tsx
priority: 0.6
keywords: [theme, themes, brand, custom, module augmentation, TLThemes, switch]
---

Register multiple named themes and switch between them at runtime.

---

Use `TLThemes` module augmentation to register custom theme IDs, then pass theme definitions via the `themes` prop. Switch the active theme with `editor.setCurrentTheme()`.
