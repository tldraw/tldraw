---
title: Audio shape
component: ./AudioShapeExample.tsx
keywords: [audio, sound, music, player, asset, assetutil, mp3, media, upload, drag, drop]
priority: 5
---

Play audio files on the canvas using a custom asset type and shape.

---

This example shows how to add audio support to the canvas. It defines a custom `AssetUtil` that accepts dropped audio files and extracts their metadata, and a custom shape that plays the audio with a native player.
