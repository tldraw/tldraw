---
title: Align and distribute shapes
component: ./AlignAndDistributeShapesExample.tsx
priority: 3
keywords:
  [
    align,
    distribute,
    alignshapes,
    distributeshapes,
    layout,
    position,
    arrange,
    horizontal,
    vertical,
  ]
---

Align and distribute shapes using the editor's built-in methods.

---

This example demonstrates how to use the `alignShapes` and `distributeShapes` methods to programmatically arrange shapes on the canvas. The example creates 5 shapes at different positions and provides buttons to align them (left, center-horizontal, right, top, center-vertical, bottom, or center on both axes) or distribute them (horizontal or vertical). Select multiple shapes and click the buttons to see the alignment and distribution in action. Align operations require at least 2 shapes, while distribute operations require at least 3 shapes.
