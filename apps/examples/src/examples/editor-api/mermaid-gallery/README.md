---
title: Mermaid fixture gallery
component: ./MermaidGalleryExample.tsx
priority: 35
keywords: [mermaid, flowchart, state diagram, import, external content]
---

Load many Mermaid examples onto the canvas at once.

---

On mount, this example clears the current page and imports all Mermaid flowchart and state fixtures on a grid using `putExternalContent({ type: 'text' })`.
