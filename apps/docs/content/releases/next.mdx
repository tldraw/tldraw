---
title: Next release
description: Changes coming in the next release
author: tldraw
date: 08/05/2026
order: 0
status: published
last_version: v5.3.0
---

This release adds a `center` alignment operation, new `AtomMap` methods matching the ones newer JavaScript runtimes add to `Map`, and faster hit-testing. It also fixes background tabs looping their sync connection, undo after a cut, and a handful of crashes and interaction bugs.

## API changes

- 🔜 **`useViewportHeight()`** is deprecated. Read `window.visualViewport` directly instead. ([#9826](https://github.com/tldraw/tldraw/pull/9826))
- Add `'center'` as an `Editor.alignShapes()` operation, which centers shapes on both axes by applying `'center-horizontal'` and `'center-vertical'` together. ([#9074](https://github.com/tldraw/tldraw/pull/9074))

  ```ts
  editor.alignShapes(editor.getSelectedShapeIds(), 'center')
  ```

- Add `getOrInsert()` and `getOrInsertComputed()` to `AtomMap`, matching the `Map` methods added in newer JavaScript runtimes. Both are reactive, and the `getOrInsertComputed()` callback only runs when the key is absent. ([#9752](https://github.com/tldraw/tldraw/pull/9752))

## Improvements

- Reduce per-pointer-event allocations in `Group2d` and `Editor` hit-testing. ([#8600](https://github.com/tldraw/tldraw/pull/8600))

## Bug fixes

- Fix background tabs repeatedly dropping and re-establishing their sync connection when the browser throttles timers. The connection health check now only resets the connection when a ping was actually sent and went unanswered. ([#9964](https://github.com/tldraw/tldraw/pull/9964))
- Fix the spacing around the cursor chat item in the context menu. ([#9921](https://github.com/tldraw/tldraw/pull/9921))
- Fix undo after a cut also reverting the change made before it. ([#9920](https://github.com/tldraw/tldraw/pull/9920))
- Fix pasted links showing a broken preview image when the linked page has no Open Graph image. ([#9909](https://github.com/tldraw/tldraw/pull/9909))
- Fix the people menu rows not lining up: the follow icon sat too close to the right edge, and collaborator names sat slightly left of your own. ([#9900](https://github.com/tldraw/tldraw/pull/9900))
- Fix the comment thread popover briefly rendering with a doubled shadow the first time a thread is opened. ([#9886](https://github.com/tldraw/tldraw/pull/9886))
- Fix the counterclockwise rotation tooltip to show its comma key. ([#9772](https://github.com/tldraw/tldraw/pull/9772))
- Fix non-finite wheel delta values reaching the camera by guarding them during wheel event normalization. ([#9586](https://github.com/tldraw/tldraw/pull/9586))
- Fix modifier keys (shift, alt, ctrl, cmd) briefly being treated as held at the start of a new click or drag right after being released, which could momentarily affect snapping and other modifier-dependent behavior. ([#9523](https://github.com/tldraw/tldraw/pull/9523))
- Fix a crash when computing a frame's geometry if its heading text couldn't be measured yet. ([#9496](https://github.com/tldraw/tldraw/pull/9496))
- Fix **Flatten to image** producing inconsistent groupings depending on selection order, and not merging clusters that were only connected through a bridging shape. ([#8661](https://github.com/tldraw/tldraw/pull/8661))
