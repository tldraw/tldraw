#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

pnpm install --frozen-lockfile
pnpm lazy run build-api
git add packages/*/api-report.api.md
pnpm lazy run build-i18n
git add apps/*/locales/*.json
git add apps/*/locales-compiled/*.json
pnpm lint-staged
