{
  "name": "@tldraw/snowpack-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "author": "@steveruizok",
  "license": "MIT",
  "private": true,
  "scripts": {
    "refresh:www": "yarn workspace @tldraw/core build && yarn workspace @tldraw/tldraw build",
    "start:www": "yarn refresh:www && yarn workspace @tldraw/www start",
    "build:www": "yarn workspace @tldraw/core build:www && yarn workspace @tldraw/tldraw build:www && yarn workspace @tldraw/www build",
    "start": "yarn workspace @tldraw/app start",
    "build": "yarn workspace @tldraw/app build",
    "test": "jest"
  },
  "workspaces": [
    "packages/*",
    "www"
  ],
  "devDependencies": {
    "@babel/plugin-syntax-import-meta": "^7.10.4",
    "@types/jest": "^26.0.24",
    "babel-jest": "^27.0.6",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4"
  },
  "dependencies": {
    "@babel/preset-react": "^7.14.5",
    "@babel/preset-typescript": "^7.15.0"
  }
}
