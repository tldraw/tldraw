// import type { TDShape } from '~types'
// import { action, computed } from 'mobx'

// export abstract class TDBaseShape<T extends TDShape> {
//   @action update = (change: Partial<T>) => {
//     Object.assign(this, change)
//   }
// }
