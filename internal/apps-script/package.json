{
	"name": "apps-scripts",
	"description": "tldraw for Google Apps",
	"version": "0.0.0",
	"private": true,
	"author": {
		"name": "tldraw Inc.",
		"email": "hello@tldraw.com"
	},
	"homepage": "https://tldraw.dev",
	"license": "SEE LICENSE IN LICENSE.md",
	"repository": {
		"type": "git",
		"url": "https://github.com/tldraw/tldraw"
	},
	"bugs": {
		"url": "https://github.com/tldraw/tldraw/issues"
	},
	"keywords": [
		"tldraw",
		"sdk",
		"drawing",
		"app",
		"development",
		"whiteboard",
		"canvas",
		"infinite"
	],
	"scripts": {
		"lint": "pnpm exec tsx ../scripts/lint.ts",
		"glogin": "clasp login",
		"glogout": "clasp logout",
		"gcreate": "clasp create --type standalone --title \"tldraw.com\" --rootDir ./dist",
		"gpull": "pnpm build && clasp pull",
		"gpush": "pnpm build && clasp push",
		"gpull:staging": "pnpm build:staging && clasp pull",
		"gpush:staging": "pnpm build:staging && clasp push",
		"build": "IS_PRODUCTION=1 pnpm exec tsx build-workspace-app.ts",
		"build:staging": "pnpm exec tsx build-workspace-app.ts",
		"build-types": "tsc --build"
	},
	"dependencies": {
		"@google/clasp": "^3.2.0"
	},
	"devDependencies": {
		"@types/google-apps-script": "^1.0.91"
	}
}
