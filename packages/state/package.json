{
	"name": "@tldraw/state",
	"description": "A tiny little drawing app (state).",
	"version": "3.11.0",
	"author": {
		"name": "tldraw Inc.",
		"email": "hello@tldraw.com"
	},
	"homepage": "https://tldraw.dev",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/tldraw/tldraw"
	},
	"bugs": {
		"url": "https://github.com/tldraw/tldraw/issues"
	},
	"keywords": [
		"tldraw",
		"drawing",
		"app",
		"development",
		"whiteboard",
		"canvas",
		"infinite"
	],
	"/* NOTE */": "These `main` and `types` fields are rewritten by the build script. They are not the actual values we publish",
	"main": "./src/index.ts",
	"types": "./.tsbuild/index.d.ts",
	"/* GOTCHA */": "files will include ./dist and index.d.ts by default, add any others you want to include in here",
	"files": [],
	"scripts": {
		"test-ci": "lazy inherit",
		"test": "pnpm jest",
		"test-coverage": "lazy inherit",
		"build": "pnpm tsx ../../internal/scripts/build-package.ts",
		"build-api": "pnpm tsx ../../internal/scripts/build-api.ts",
		"prepack": "pnpm tsx ../../internal/scripts/prepack.ts",
		"postpack": "../../internal/scripts/postpack.sh",
		"pack-tarball": "pnpm pack",
		"lint": "pnpm tsx ../../internal/scripts/lint.ts"
	},
	"jest": {
		"preset": "../../internal/config/jest/node/jest-preset.js",
		"setupFiles": [
			"raf/polyfill"
		],
		"moduleNameMapper": {
			"^~(.*)": "<rootDir>/src/$1"
		}
	},
	"devDependencies": {
		"@types/lodash": "^4.17.14",
		"lodash": "^4.17.21"
	},
	"dependencies": {
		"@tldraw/utils": "workspace:*"
	},
	"typedoc": {
		"readmeFile": "none",
		"entryPoint": "./src/index.ts",
		"displayName": "@tldraw/state",
		"tsconfig": "./tsconfig.json"
	}
}
