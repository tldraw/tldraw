import * as React from 'react';
import { TLBoundsEdge, TLBounds } from '../../../types';
export declare const EdgeHandle: React.MemoExoticComponent<({ size, bounds, edge }: {
    size: number;
    bounds: TLBounds;
    edge: TLBoundsEdge;
}) => JSX.Element>;
