## API Report File for "@tldraw/headless"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Editor } from '@tldraw/editor';
import { TLAnyBindingUtilConstructor } from '@tldraw/editor';
import { TLAnyShapeUtilConstructor } from '@tldraw/editor';
import { TldrawOptions } from '@tldraw/editor';
import { TLEditorSnapshot } from '@tldraw/editor';
import { TLPersistentClientSocket } from '@tldraw/sync-core';
import { TLRecord } from '@tldraw/tlschema';
import { TLSocketStatusChangeEvent } from '@tldraw/sync-core';
import { TLStateNodeConstructor } from '@tldraw/editor';
import { TLStore } from '@tldraw/tlschema';
import { TLStoreSnapshot } from '@tldraw/editor';
import { TLSyncClient } from '@tldraw/sync-core';
import { TLTextMeasurer } from '@tldraw/editor';
import { TLTextOptions } from '@tldraw/editor';

// @public
export function connectHeadlessEditor(editor: Editor, opts: TLHeadlessConnectOptions): Promise<TLHeadlessConnection>;

// @public
export function createHeadlessEditor(opts?: TLHeadlessEditorOptions): Editor;

// @public
export function ensureHeadlessDocument(): void;

// @public
export class NodeWebSocketAdapter implements TLHeadlessClientSocket {
    constructor(getUri: () => Promise<string> | string, opts?: NodeWebSocketAdapterOptions);
    // (undocumented)
    close(): void;
    // (undocumented)
    connectionStatus: 'error' | 'offline' | 'online';
    // (undocumented)
    onReceiveMessage(cb: (msg: any) => void): () => void;
    // (undocumented)
    onStatusChange(cb: (event: TLSocketStatusChangeEvent) => void): () => void;
    // (undocumented)
    restart(): void;
    // (undocumented)
    sendMessage(msg: object): void;
}

// @public (undocumented)
export interface NodeWebSocketAdapterOptions {
    maxReconnectDelay?: number;
    minReconnectDelay?: number;
}

// @public
export type TLHeadlessClientSocket = TLPersistentClientSocket<any, any>;

// @public (undocumented)
export interface TLHeadlessConnection {
    // (undocumented)
    readonly client: TLSyncClient<TLRecord, TLStore>;
    close(): void;
    flush(timeoutMs?: number): Promise<void>;
    // (undocumented)
    readonly socket: TLHeadlessClientSocket;
}

// @public (undocumented)
export interface TLHeadlessConnectOptions {
    connectTimeout?: number;
    onSyncError?(reason: string): void;
    socket?: TLHeadlessClientSocket;
    uri?: (() => Promise<string> | string) | string;
    userInfo?: {
        color?: string;
        id?: string;
        name?: string;
    };
}

// @public (undocumented)
export interface TLHeadlessEditorOptions {
    bindingUtils?: readonly TLAnyBindingUtilConstructor[];
    editorOptions?: Partial<TldrawOptions>;
    frameLoop?: 'auto' | 'manual';
    licenseKey?: string;
    shapeUtils?: readonly TLAnyShapeUtilConstructor[];
    snapshot?: TLEditorSnapshot | TLStoreSnapshot;
    textMeasurer?: TLTextMeasurer;
    textOptions?: null | TLTextOptions;
    tools?: readonly TLStateNodeConstructor[];
    viewport?: {
        height: number;
        width: number;
    };
}

// (No @packageDocumentation comment for this package)

```
