// `CommentAuthor` is defined in @tldraw/mentions (the lower-level package that owns Avatar and the
// mention picker) and re-exported here so the commenting public API is unchanged.
export { type CommentAuthor } from '@tldraw/mentions'
