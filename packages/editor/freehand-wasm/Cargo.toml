[package]
name = "freehand_wasm"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
libm = "0.2"

[profile.release]
opt-level = 3
lto = true
panic = "abort"
strip = true
codegen-units = 1
