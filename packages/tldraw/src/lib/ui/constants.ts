// Breakpoints for portrait, keep in sync with PORTRAIT_BREAKPOINT enum below!
export const PORTRAIT_BREAKPOINTS = [0, 360, 400, 440, 480, 580, 640, 840, 1023]

// Mapping for above array -- needs to be kept in sync!
export enum PORTRAIT_BREAKPOINT {
	ZERO = 0,
	MOBILE_XXXS = 1,
	MOBILE_XXS = 2,
	MOBILE_XS = 3,
	MOBILE_SM = 4,
	MOBILE = 5,
	TABLET_SM = 6,
	TABLET = 7,
	DESKTOP = 8,
}
