import { Sun } from '..'

describe('Post-It shape', () => {
  it('Creates a shape', () => {
    expect(Sun.create).toBeDefined()
    // expect(Sticky.create({ id: 'sticky' })).toMatchSnapshot('sticky')
  })
})
