import * as React from 'react';
interface ContextMenuProps {
    children: React.ReactNode;
}
export declare const ContextMenu: React.MemoExoticComponent<({ children }: ContextMenuProps) => JSX.Element>;
export {};
